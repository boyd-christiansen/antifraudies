# Phase 2 stage: near_duplicate (placeholder, NOT implemented)

Whole-image near-duplicate detection — find one image reused across products (perceptual hashing over the content-addressed store).

Reads from / writes to the shared phase-1 evidence store. Surfaces apparent anomalies for human review; never renders a verdict. See ../README.md for the full funnel.
