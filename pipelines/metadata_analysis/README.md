# Phase 2 stage: metadata_analysis (placeholder, NOT implemented)

Metadata & filename analysis — cheap reuse / bookkeeping tells (e.g. shared timestamps across unrelated products). Runs on the whole corpus.

Reads from / writes to the shared phase-1 evidence store. Surfaces apparent anomalies for human review; never renders a verdict. See ../README.md for the full funnel.
