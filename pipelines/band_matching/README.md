# Phase 2 stage: band_matching (placeholder, NOT implemented)

Cross-catalog band matching — find bands copied/reused under flips/rotations; treat extracted bands as a DB searched against itself.

Reads from / writes to the shared phase-1 evidence store. Surfaces apparent anomalies for human review; never renders a verdict. See ../README.md for the full funnel.
