# Phase 2 stage: background_fingerprint (placeholder, NOT implemented)

Background-template fingerprinting — find one background reused across many products with only a band edited in.

Reads from / writes to the shared phase-1 evidence store. Surfaces apparent anomalies for human review; never renders a verdict. See ../README.md for the full funnel.
